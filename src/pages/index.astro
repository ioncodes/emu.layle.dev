---
import { getCommits, getEmulators } from "@/lib/meta";
import "../styles/global.css";

function iconUrl(slug: string, color: string): string {
  const [prefix, name] = slug.split(":");
  return `https://api.iconify.design/${prefix}/${name}.svg?color=${encodeURIComponent(color)}`;
}

const emulators = getEmulators().map((e) => {
  const commits = getCommits(e.slug);

  return {
    ...e,
    latestCommit: commits[0]?.commit_short ?? null,
    iconSrc: e.icon ? iconUrl(e.icon, e.accent_color) : null,
  };
});

const mine = emulators.filter((e) => !e.author);
const friends = emulators.filter((e) => !!e.author);
---

<!doctype html>
<html lang="en" class="bg-white text-neutral-900 dark:bg-neutral-950 dark:text-neutral-100">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>emu.layle.dev</title>
  </head>
  <body class="min-h-screen font-sans antialiased">
    <main class="mx-auto max-w-3xl px-6 py-16">
      <h1 class="text-2xl font-semibold">emu.layle.dev</h1>
      <p class="mt-2 text-sm text-neutral-600 dark:text-neutral-400">
        Per-commit screenshot snapshots for emulators. Useful to track regressions and improvements over time.
      </p>

      <h2 class="mt-10 text-xs font-semibold uppercase tracking-wide text-neutral-500">Mine</h2>
      <ul class="mt-3 space-y-3">
        {
          mine.map((e) => (
            <li>
              <a
                href={e.latestCommit ? `/${e.slug}/${e.latestCommit}` : undefined}
                aria-disabled={!e.latestCommit}
                class:list={[
                  "block rounded border p-4",
                  e.latestCommit
                    ? "border-neutral-200 hover:bg-neutral-50 dark:border-neutral-800 dark:hover:bg-neutral-900"
                    : "border-dashed border-neutral-200 text-neutral-400 dark:border-neutral-800 dark:text-neutral-600 cursor-not-allowed",
                ]}
              >
                <div class="flex items-center gap-3">
                  {e.iconSrc ? (
                    <img
                      src={e.iconSrc}
                      alt=""
                      width="20"
                      height="20"
                      loading="lazy"
                      class="h-5 w-5 shrink-0"
                    />
                  ) : (
                    <span
                      class="inline-block h-2 w-2 rounded-full"
                      style={`background-color: ${e.accent_color};`}
                      aria-hidden="true"
                    />
                  )}
                  <span class="font-medium">{e.name}</span>
                  <span class="text-xs text-neutral-500">{e.console}</span>
                  {!e.latestCommit && (
                    <span class="ml-auto text-xs text-neutral-400">no submissions yet</span>
                  )}
                </div>
                <p class="mt-1 text-sm text-neutral-600 dark:text-neutral-400">{e.description}</p>
              </a>
            </li>
          ))
        }
      </ul>

      {
        friends.length > 0 && (
          <>
            <h2 class="mt-10 text-xs font-semibold uppercase tracking-wide text-neutral-500">
              From friends
            </h2>
            <ul class="mt-3 space-y-3">
              {friends.map((e) => (
                <li>
                  <a
                    href={e.latestCommit ? `/${e.slug}/${e.latestCommit}` : undefined}
                    aria-disabled={!e.latestCommit}
                    class:list={[
                      "block rounded border p-4",
                      e.latestCommit
                        ? "border-neutral-200 hover:bg-neutral-50 dark:border-neutral-800 dark:hover:bg-neutral-900"
                        : "border-dashed border-neutral-200 text-neutral-400 dark:border-neutral-800 dark:text-neutral-600 cursor-not-allowed",
                    ]}
                  >
                    <div class="flex items-center gap-3">
                      {e.iconSrc ? (
                        <img
                          src={e.iconSrc}
                          alt=""
                          width="20"
                          height="20"
                          loading="lazy"
                          class="h-5 w-5 shrink-0"
                        />
                      ) : (
                        <span
                          class="inline-block h-2 w-2 rounded-full"
                          style={`background-color: ${e.accent_color};`}
                          aria-hidden="true"
                        />
                      )}
                      <span class="font-medium">{e.name}</span>
                      <span class="text-xs text-neutral-500">{e.console}</span>
                      <span class="text-xs text-neutral-500">by {e.author}</span>
                      {!e.latestCommit && (
                        <span class="ml-auto text-xs text-neutral-400">no submissions yet</span>
                      )}
                    </div>
                    <p class="mt-1 text-sm text-neutral-600 dark:text-neutral-400">{e.description}</p>
                  </a>
                </li>
              ))}
            </ul>
          </>
        )
      }
    </main>
  </body>
</html>
